

// get input numbers
const n = parseInt(prompt("Enter the value of n: "));
var sum = 0;

//add two numbers
for(i = 0;i<=n;i++)
{
    sum+=i;
}

// display the sum
console.log(`The sum is ${sum}`); //check console to see the answer....!!!
prompt(sum); //will prompt the answer...!

